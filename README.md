Убрал раздел "Расширение системы". Вот обновленная документация:

# 🏢 Universal Booking System - Документация

## 📖 Оглавление
1. [Обзор системы](#-обзор-системы)
2. [Архитектура](#-архитектура)
3. [Быстрый старт](#-быстрый-старт)
4. [API Документация](#-api-документация)
5. [Типы ресурсов](#-типы-ресурсов)
6. [Расписания](#-расписания)
7. [Бронирования](#-бронирования)
8. [Стратегии слотов](#-стратегии-слотов)
9. [События и уведомления](#-события-и-уведомления)
10. [Команды Artisan](#-команды-artisan)

## 🎯 Обзор системы

**Universal Booking System** - это гибкая система бронирования ресурсов с поддержкой различных типов расписаний, стратегий управления временными слотами и комплексной бизнес-логикой.

### 🚀 Ключевые возможности

- ✅ **Мультитенантность** - поддержка нескольких компаний
- ✅ **Гибкие расписания** - статические и динамические
- ✅ **Разные типы ресурсов** - сотрудники, переговорные, оборудование
- ✅ **Автоматическая генерация слотов** - фиксированные и динамические стратегии
- ✅ **Подтверждение броней** - автоматическое и ручное
- ✅ **Отмена и перенос** - с ограничениями по времени
- ✅ **Групповые бронирования** - для тренировок и мероприятий
- ✅ **REST API** - полное API для интеграций
- ✅ **Тестовые данные** - генерация реалистичных данных

## 🏗️ Архитектура

### Модели данных

```
Company (Компания)
├── Timetable (Расписание)
├── ResourceType (Тип ресурса)
│   └── Resource (Ресурс)
└── Booking (Бронирование)
    └── Booker (Бронирующий)
```

### Сервисный слой

- **BookingService** - основная бизнес-логика бронирований
- **SlotGenerationService** - генерация доступных слотов
- **ArtisanCommandService** - утилиты для генерации данных

### Паттерны

- **Action Classes** - для отдельных операций
- **Value Objects** - для конфигурации ресурсов
- **Events/Listeners** - для уведомлений
- **Repository** - через Eloquent модели

## 🚀 Быстрый старт

### 1. Установка

```bash
# Клонировать проект
git clone <repository>
cd booking-system

# Установить зависимости
composer install

# Настроить окружение
cp .env.example .env
php artisan key:generate

# Запустить скрипт установки (если нужно)
chmod +x setup_booking_system.sh
./setup_booking_system.sh
```

### 2. База данных

```bash
# Настроить .env файл
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=booking
DB_USERNAME=booking
DB_PASSWORD=booking

# Выполнить миграции
php artisan migrate

# Сгенерировать тестовые данные
php artisan booking:generate-test-data
```

### 3. Запуск

```bash
# Запустить сервер
php artisan serve

# Или использовать Docker
cd Docker
docker-compose up -d
```

## 📡 API Документация

### Базовый URL
```
http://localhost:8000/api
```

### Основные endpoints

#### 🔧 Ресурсы
```http
GET    /api/booking/resources
GET    /api/booking/{resource}/slots
GET    /api/booking/check
```

#### 📅 Бронирования
```http
POST   /api/booking/create
POST   /api/booking/{id}/confirm
POST   /api/booking/{id}/cancel
POST   /api/booking/{id}/reschedule
GET    /api/booking/resource/{id}/bookings
```

#### 🏢 Управление компаниями
```http
GET    /api/companies
POST   /api/companies
PUT    /api/companies/{id}
DELETE /api/companies/{id}
```

### Примеры запросов

#### Создание бронирования
```bash
curl -X POST http://localhost:8000/api/booking/create \
  -H "Content-Type: application/json" \
  -d '{
    "resource_id": 1,
    "start": "2024-01-15 14:00:00",
    "end": "2024-01-15 15:00:00",
    "booker": {
      "external_id": "client_123",
      "type": "client",
      "name": "John Doe",
      "email": "john@example.com",
      "phone": "+1234567890"
    }
  }'
```

#### Получение доступных слотов
```bash
curl "http://localhost:8000/api/booking/1/slots?date=2024-01-15&count=5"
```

## 🎪 Типы ресурсов

### 1. Сотрудник (Employee)
- **Назначение**: Персональные консультации, услуги
- **Слоты**: Фиксированные (60 минут)
- **Подтверждение**: Автоматическое
- **Пример**: Парикмахер, массажист, косметолог

### 2. Переговорная (Meeting Room)
- **Назначение**: Встречи, переговоры
- **Слоты**: Динамические (30 минут)
- **Подтверждение**: Требуется подтверждение
- **Пример**: Комната для совещаний

### 3. Тренировка (Training)
- **Назначение**: Групповые занятия
- **Слоты**: Фиксированные (90 минут)
- **Подтверждение**: Автоматическое
- **Пример**: Йога, фитнес, мастер-классы

### 4. Оборудование (Equipment)
- **Назначение**: Специальная техника
- **Слоты**: Динамические (120 минут)
- **Подтверждение**: Требуется подтверждение
- **Пример**: 3D принтер, проектор

## 📅 Расписания

### Статические расписания

Работают по дням недели с фиксированными рабочими часами.

**Пример конфигурации:**
```json
{
  "days": {
    "monday": {
      "working_hours": {
        "start": "09:00",
        "end": "18:00"
      },
      "breaks": [
        {
          "start": "13:00",
          "end": "14:00"
        }
      ]
    }
  },
  "holidays": ["01-01", "03-08", "05-01"]
}
```

### Динамические расписания

Работают по конкретным датам с индивидуальными настройками.

**Пример конфигурации:**
```json
{
  "dates": {
    "01-15": {
      "working_hours": {
        "start": "10:00",
        "end": "19:00"
      },
      "breaks": [
        {
          "start": "14:00",
          "end": "15:00"
        }
      ]
    }
  }
}
```

## ⏰ Бронирования

### Статусы бронирований

```php
BookingStatus::PENDING;           // Ожидает подтверждения
BookingStatus::CONFIRMED;         // Подтверждена
BookingStatus::CANCELLED_BY_CLIENT; // Отменена клиентом
BookingStatus::CANCELLED_BY_ADMIN;  // Отменена администратором
BookingStatus::REJECTED;          // Отклонена
```

### Процесс бронирования

1. **Проверка доступности** - проверка конфликтов
2. **Валидация времени** - минимальное время для брони
3. **Создание брони** - с учетом стратегии подтверждения
4. **Прикрепление бронирующего** - создание/привязка клиента
5. **Уведомления** - отправка событий

### Ограничения по времени

```php
$config = [
    'min_advance_time' => 60,     // Минут до брони
    'cancellation_time' => 120,   // Минут для отмены
    'reschedule_time' => 240,     // Минут для переноса
];
```

## 🎯 Стратегии слотов

### Фиксированные слоты (Fixed)

Слоты генерируются заранее на основе расписания.

**Алгоритм:**
1. Получить рабочие часы для даты
2. Разделить на интервалы по `slot_duration_minutes`
3. Исключить периоды перерывов
4. Вернуть массив доступных слотов

**Преимущества:**
- Предсказуемое расписание
- Простота для клиентов
- Легкая визуализация

### Динамические слоты (Dynamic)

Слоты генерируются на основе текущей занятости.

**Алгоритм:**
1. Получить рабочие часы для даты
2. Получить существующие бронирования
3. Вычислить свободные периоды
4. Исключить перерывы
5. Разделить свободные периоды на слоты

**Преимущества:**
- Максимальное использование времени
- Гибкость для администраторов
- Учет реальной занятости

## 🔔 События и уведомления

### Доступные события

```php
BookingCreated::class;        // Создана новая бронь
BookingConfirmed::class;      // Бронь подтверждена
BookingCancelled::class;      // Бронь отменена
BookingRescheduled::class;    // Бронь перенесена
BookingReminder::class;       // Напоминание о брони
```

### Пример обработчика

```php
class BookingCreatedListener
{
    public function handle(BookingCreated $event)
    {
        // Отправка email уведомления
        // Логирование в систему
        // Уведомление администратору
    }
}
```

## 🛠️ Команды Artisan

### Генерация тестовых данных

```bash
php artisan booking:generate-test-data --company-id=1
```

**Создает:**
- Компанию с ID=1
- Статическое и динамическое расписания
- 4 типа ресурсов
- 8 конкретных ресурсов
- Тестовые бронирования

### Генерация расписаний

```bash
# Статическое расписание
php artisan timetable:generate-static 1

# Динамическое расписание (на 14 дней)
php artisan timetable:generate-dinamic 1 14
```

### Утилиты

```bash
# Очистка тестовых данных
php artisan migrate:fresh

# Повторная генерация
php artisan booking:generate-test-data
```

## ⚙️ Конфигурация ресурсов

### Базовая конфигурация

```php
$config = [
    'slot_duration_minutes' => 60,
    'slot_strategy' => 'fixed', // или 'dinamic'
    'require_confirmation' => false,
    'min_advance_time' => 60,
    'cancellation_time' => 120,
    'reschedule_time' => 240,
    'max_participants' => null, // для групповых броней
];
```

### Наследование конфигурации

Ресурсы наследуют конфигурацию от своего типа, но могут переопределять отдельные параметры:

```php
// Тип ресурса
ResourceType::create([
    'resource_config' => [
        'slot_duration_minutes' => 60,
        'require_confirmation' => true,
    ]
]);

// Конкретный ресурс (переопределяет длительность слота)
Resource::create([
    'resource_config' => [
        'slot_duration_minutes' => 45 // Переопределено
    ]
]);
```

## 🔄 Рабочие процессы

### Процесс бронирования клиентом

1. **Поиск ресурса** → `GET /api/booking/resources`
2. **Проверка слотов** → `GET /api/booking/{id}/slots`
3. **Создание брони** → `POST /api/booking/create`
4. **Ожидание подтверждения** (если требуется)
5. **Получение уведомления** о статусе

### Процесс управления администратором

1. **Просмотр броней** → `GET /api/booking/resource/{id}/bookings`
2. **Подтверждение/отклонение** → `POST /api/booking/{id}/confirm/cancel`
3. **Управление ресурсами** → CRUD API
4. **Настройка расписаний** → Timetable API

## 🐳 Docker развертывание

### Запуск с Docker

```bash
cd Docker
docker-compose up -d
```

### Доступные сервисы

- **MySQL**: `localhost:8006` (база данных)
- **phpMyAdmin**: `localhost:8093` (веб-интерфейс)

### Настройка .env для Docker

```env
DB_HOST=booking
DB_PORT=3306
DB_DATABASE=booking
DB_USERNAME=booking
DB_PASSWORD=booking
```

---

## 🆘 Поддержка

### Частые проблемы

**Проблема**: "Слоты не генерируются"
**Решение**: Проверить расписание и рабочие часы ресурса

**Проблема**: "Ошибка валидации времени"
**Решение**: Проверить `min_advance_time` в конфигурации

**Проблема**: "Конфликты бронирований"
**Решение**: Использовать `GET /api/booking/check` перед созданием

### Логирование

Система использует отдельный канал логирования:
```php
Log::channel('booking')->info('Сообщение');
```

Логи находятся в: `storage/logs/booking.log`

---

**🎉 Поздравляем!** Теперь у вас есть полнофункциональная система бронирования готовая к использованию в продакшене.
